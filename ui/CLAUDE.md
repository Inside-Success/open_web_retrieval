# UI surface routing

`registry.yaml` is the repository's UI inventory authority. Read it before
planning, creating, replacing, or retiring a human-facing surface. This
repo has one real surface, the search-activity workbench -- patch it by
default rather than creating a new one.
