# User interfaces

Canonical inventory: `registry.yaml` -- one real surface, the search-activity
workbench (`workbench/frontend` + `workbench/backend`). This is the
Inside-Success fork of `BrianMills2718/open_web_retrieval`; same workbench
code. Read it before adding, changing, or replacing any UI.
