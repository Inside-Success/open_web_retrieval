# UI surface routing

Generated derivative of `CLAUDE.md`. Read `CLAUDE.md` and `registry.yaml`
before UI work; update the canonical file and regenerate this projection when
the local routing contract changes.
